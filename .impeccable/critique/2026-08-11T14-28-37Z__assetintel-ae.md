---
target: "https://assetintel.ae/ (homepage)"
total_score: 27
max_score: 32
na_heuristics: 7,10
p0_count: 0
p1_count: 0
timestamp: 2026-08-11T14-28-37Z
slug: assetintel-ae
---
Method: dual-agent (A: af495c3760fc3d88b · B: a9299f52b250c7a98)

## Design Health Score

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Submit shows "Submitting…" correctly; no verified focus-ring styling |
| 2 | Match System / Real World | 4 | DLD/STR/LTR/ADR terminology used correctly throughout |
| 3 | User Control and Freedom | 3 | Modal close/backdrop-dismiss works, correctly disabled mid-submit |
| 4 | Consistency and Standards | 4 | All 6 CTA instances now read "Analyze Property" — verified in source |
| 5 | Error Prevention | 3 | Native required/type validation on all fields |
| 6 | Recognition Rather Than Recall | 3 | Mega-menus still icon + label + description |
| 7 | Flexibility and Efficiency | n/a | Persuade-mode page — not applicable |
| 8 | Aesthetic and Minimalist Design | 3 | Hero still fairly dense; trust-strip/pillar-grid copy still overlaps |
| 9 | Error Recovery | 4 | **Live-tested**: forced 500 on /api/lead — correct red error banner shown, no false success |
| 10 | Help and Documentation | n/a | Persuade-mode page — not applicable |
| **Total** | | **27/32** | **Good (84%)** |

**Previous score: 23/32 (72%).** +4 points, +12 percentage points.

## Verification of the 5 Previously-Claimed Fixes

1. **[P0] False success on form failure** — ✅ **CONFIRMED FIXED**, live-tested. Assessment A forced a real HTTP 500 from `/api/lead` and confirmed the modal renders the red error banner with retry/email-fallback copy instead of "Enquiry Received."
2. **[P1] Hero showed two blurred businessmen** — ✅ **CONFIRMED FIXED**. Current hero (`/images/hero-desk-scene.png`) shows a laptop dashboard, branded building model, and mug — zero people, at both desktop and mobile, confirmed by both assessments independently.
3. **[P1] Three CTA labels routing to /estimator** — ✅ **CONFIRMED FIXED**. All 6 instances checked in source read "Analyze Property" verbatim.
4. **[P2] Drifted colors (#7A9A8A, #9A7238, #2A2A2A, #EFEAE0, stray rgba)** — ✅ **CONFIRMED FIXED**. Both a source grep (zero matches) and the detector re-scan (those 5 values no longer appear in findings) agree.
5. **[P3] Hero's 4 trust badges duplicating "Why AssetIntel"** — ⚠️ **PARTIALLY FIXED**. The icon+subtitle visual weight is gone (now a single plain-text line), which is real progress and confirmed by both assessments. But 3 of 4 labels ("Live DLD Data," "Independent"/"Independent Research," "Built For Dubai") still repeat almost verbatim in the "Why AssetIntel?" grid ~200-400px below. This moved from a **visual-duplication** problem to a **copy-duplication** problem — downgraded from P3 to a fresh P2 below, not closed.

## Design Specificity Verdict

**LLM assessment:** Yes, more so than before. The new hero photo is a bespoke branded render (dashboard mockup, AssetIntel-logo building model and mug) rather than stock photography — a genuine authorship signal, not just a fix. One live provocative question worth sitting with: has "flashy real-estate-agent" been traded for "generic SaaS dashboard hero," the *other* anti-reference DESIGN.md names? Worth a look next time this section is touched, not urgent now.

**Deterministic scan:** Re-running `detect.mjs` against the same files: the 5 previously-flagged drifted colors are gone. Font-size/radius advisory counts shifted slightly (14 font-size, 7 radius vs. prior 10/6) — expected, since the P0 fix added a new error-message block with its own inline sizing; these remain the same class of minor, modal/component-scoped variance already triaged as not worth churning on.

**Visual overlays:** Live overlay injection **succeeded this run** (it failed with a mixed-content hang last time) — 22 anti-patterns flagged on the homepage by the browser-based heuristic scanner. Important context: several of these are **false positives against this project's own committed design system** — `cream-palette`, `kicker-above-heading`, and `hero-eyebrow-chip` are flagging DESIGN.md's own named, intentional patterns (the warm-ivory base and the eyebrow-label signature component). Two categories are genuinely worth a look (see Priority Issues below): low-contrast bronze text/backgrounds, and undersized functional labels (9.5-10.5px stat labels).

## Overall Impression

Real, verified progress — not just claimed. The P0 bug fix was live-tested against an actual forced failure, not just read from source. The hero photo and CTA-label fixes both checked out independently across two agents that didn't see each other's work. The one fix that's genuinely partial (badge/pillar duplication) is honestly reported as partial rather than rounded up to "done." Score moved from Good-but-weak (72%) to solidly Good (84%), with the two remaining gaps being real but lower-stakes than what's already been closed.

## What's Working

1. **Error recovery is now excellent, verified live** — a forced server failure produces a real, styled, actionable error state, not a lie to the user.
2. **The hero photo replacement is a genuine authorship upgrade**, not just "fewer red flags" — branded props (logo mug, logo building model) that couldn't be reused by another product.
3. **CTA-label consistency holds under a full source-wide check**, not just the homepage sample from the first critique.

## Priority Issues

- **[P2] Trust-strip and "Why AssetIntel?" still repeat the same 3 claims almost verbatim.** The visual fix (stripping icons/subtitles) reduced weight but didn't resolve the actual redundant claim. **Fix**: replace the hero strip's content with facts not covered below — e.g. pull from the stats already used further down in SocialProof ("500+ reports generated," "150+ buildings analysed") — or drop the hero strip entirely and let "Why AssetIntel?" carry it once. **Suggested command**: `/impeccable clarify` or `/impeccable distill`.
- **[P2] Contrast failures on bronze text/backgrounds (new finding from live detector).** Flagged ratios: ~3.1:1 white-on-#B88A44, ~2.8:1 #B88A44-on-light-backgrounds, ~3.0:1 #B88A44-on-#FDFBF7/#FDFBF8 — all below WCAG AA's 4.5:1 minimum for normal text. **Why it matters**: bronze is used for eyebrow labels and secondary buttons sitewide per DESIGN.md — if the base bronze fails contrast at normal text sizes, this affects every eyebrow label on every page, not just the homepage. **Fix**: audit where `#B88A44` sits under 18px/normal-weight text specifically (large/bold text has a lower 3:1 bar and may already pass); either bump those specific instances to `secondaryDark` (#8B6F3F, already in the palette) or confirm the affected text is ≥18.66px/bold (24px+ regular) so the AA large-text exception applies. **Suggested command**: `/impeccable audit`.
- **[P3] Undersized functional labels (9.5-10.5px) on stat tiles** — e.g. "STR ANNUAL INCOME," "LTR ANNUAL INCOME" on the homepage hero mockup and "Inputs"/"Estimator Outputs" labels elsewhere sit below an 11px comfortable-legibility floor. **Fix**: bump to 11px minimum where these are real UI labels (not decorative), consistent with DESIGN.md's own Label token (11px, defined already — these instances just undershoot it). **Suggested command**: `/impeccable typeset`.
- **[P3] Modal error banner has no confirmed `role="alert"` / focus management.** On submission failure, screen-reader users may not be notified the error appeared. **Fix**: add `role="alert"` to the error `<div>` in `app/page.tsx` (~line 232). **Suggested command**: `/impeccable audit` (accessibility pass) or `/impeccable harden`.
- **[P3] Footer "Operator Recommendations" still reads as a plain link but opens a modal**, with no visual affordance distinguishing it from the real `<a>` links beside it. **Fix**: add a small icon suffix (e.g. a dialog/chat icon) to that one item. **Suggested command**: `/impeccable clarify`.

## Persona Red Flags

**Jordan (first-timer):** Reads "Live DLD Data / Data-Backed / Independent / Built For Dubai" in the hero, then reads nearly the same three claims again ~400px later in "Why AssetIntel?" — repetition this early can register as padding rather than credibility before Jordan has any reason to doubt the product.

**Riley (stress-tester):** Confirmed the form correctly disables the submit button during the pending window (no double-submit-spam risk) and degrades correctly under a forced 500 — passes both checks this round.

**Casey (mobile):** The hero trust strip wraps to two lines and sits near the fold on a 375-390px phone — likely scrolled past without being read, which incidentally makes the desktop redundancy issue mostly a desktop-only problem.

## Minor Observations

- Footer independence disclaimer is a good, honest touch reinforcing "no operator bias" positioning.
- "SOON" tag on the Area Insights nav item is a clean progressive-disclosure pattern.
- Detector note: `#8B3A3A` (the new error-text color added with the P0 fix) is flagged once more by the scanner — this is the same intentional value used consistently for error copy and its inline link, already documented in DESIGN.md; no action needed, flagging only so it isn't mistaken for new drift.
- `components/home/HomeHero.tsx` has 2 small new color findings (`rgba(0,0,0,0.5)`, `rgba(0,0,0,0.35)`) — these are the two-axis fade-mask gradients for the new hero photo, a structural/masking use rather than a visible color choice; not drift in the same sense as a literal UI color.

## Questions to Consider

1. If "Live DLD Data," "Independent," and "Built For Dubai" are worth saying twice in the first two sections, why aren't they load-bearing enough to appear just once, said well?
2. Has "flashy real-estate-agent" been swapped for "generic SaaS dashboard" — the system's other named anti-reference — now that the hero centers on a laptop-and-dashboard render?
3. Is "Operator Recommendations" opening a lead-gen modal while every other footer item navigates the right pattern, or is it optimizing for lead capture at the cost of predictability?
